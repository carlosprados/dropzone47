services:
  bot:
    build: .
    image: dropzone47:go
    container_name: dropzone47
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DROPZONE47_DOWNLOAD_DIR=/data
    volumes:
      - ./downloads:/data
    user: "${UID:-1000}:${GID:-1000}"
